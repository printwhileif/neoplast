
# Dating App Card Stack

Projeto exemplo "Dating App Card Stack" baseado no design original do Figma.

Original design: https://www.figma.com/file/Bx1uiTfEFzruj2Do8eHVDe/Dating-App-Card-Stack

## Descrição

Bundle de componentes React + Vite que implementa um card stack para um app de encontros.

## Requisitos

- Node.js 18+ recomendado
- npm (ou pnpm/yarn)

## Como rodar

Instale dependências:

```bash
npm install
```

Inicie o servidor de desenvolvimento (Vite):

```bash
npm run dev
```

Abra `http://localhost:3000/` no navegador.

## Licença

Este repositório não inclui uma licença explícita. Adicione uma `LICENSE` se quiser publicar o código.

## Observações

- Para expor o servidor na rede local: `npm run dev -- --host`.
- Se quiser que eu crie o repositório no GitHub, informe o nome do repositório e se deve ser `public` ou `private`, e se prefere autenticar via GitHub CLI (`gh`) ou me fornecer um token pessoal (não recomendado compartilhar tokens em chats públicos).
  